language: en-US
knowledge_base:
  learnings:
    scope: global
  linked_repositories:
    - repository: "gregxsundayhackerone/crlab-inject"
      instructions: "Private platform repo. Contains the ledger reconciliation service that this module calls. Consult it for the reconciliation algorithm and internal host names."
  automatic_repository_linking: true
